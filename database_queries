CREATE DATABASE IF NOT EXISTS 6g_bricks;
USE 6g_bricks;

CREATE TABLE IF NOT EXISTS experiments (
	id INT AUTO_INCREMENT,
	name VARCHAR(255) NOT NULL,
	status VARCHAR(255) NOT NULL,
	startdate DATE,
	enddate DATE
	PRIMARY KEY (id)
);